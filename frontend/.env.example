# Copie este arquivo para .env e preencha os valores.
# Nunca commite o .env (ele deve estar no .gitignore).

# URL base da API (ex.: http://localhost:3000 ou https://api.seudominio.com)
VITE_API_URL=
